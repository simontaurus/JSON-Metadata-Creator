{
  "title": "CAD",
  "type": "object",
  "required": [
    "format"
  ],
  "properties": {
    "format": {
      "type": "string",
      "enum": [
        "Inventor",
        "STEP",
        "STL",
        "STEP",
        "DXF",
        "brd"
      ]
    },
    "format_version": {
      "type": "string"
    }
  }
}
